public class Graph{
    // It can be represented in adjacency matrix and adjacency list.
    // Adjacency Matrix - O(N*N), where N is the no of vertices
    // Adjacency List - O(2*M), where M is the no of edges

    public static void main(String[] args){
        ArrayList<ArrayList<Integer>> adj = new ArrayList<ArrayList<Integer>>();
        for(int i=0;i<=n;i++)
            adj.add(new ArrayList<Integer>());
        // Adding elements
        // adj.get(i).add(j);
        // adj.get(j).add(i);
    }

}